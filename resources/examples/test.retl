let arr = [1, 2, 3, 4];
writeCSV("temp", {});
temp_schema: schema = {
    num: string
};
let arr2 = readCSV("temp", temp_schema)["num"] |>
            filter(|x| = {
                match x {
                    case x: int => true
                    _ => false
                }
            });
println("Even nums: {}", arr2)