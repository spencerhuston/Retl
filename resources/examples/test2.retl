let arr = [1, 2, 3, 4];
writeCSV("temp", ${}, false);
let temp_schema: schema = ${
    num: string
};
let arr2 = readCSV("temp", temp_schema);
let arr3 = filter(arr2("num"), |x| -> bool {
                match x {
                    case x: int if x % 2 == 0 => true,
                    case _ => false
                }
            })
            |>
            map(|x| -> int {
                x * 2
            });
println("Even nums: {}", arr2)